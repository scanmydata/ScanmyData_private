Flask>=2.3
gunicorn>=20.1
requests>=2.28
eventlet>=0.35.0
flask-sock>=0.2.0
websocket-client>=1.8.0
xmltodict>=0.13.0
pandas>=2.0
openpyxl>=3.0
python-dotenv>=1.0
markupsafe>=2.1
Pillow>=9.0
# pyzbar is optional; we rely on OpenCV fallback to avoid zbar system lib
# pyzbar>=0.1.9
xsdata>=24.0
beautifulsoup4>=4.12
lxml>=4.9
xlrd>=1.2.0
xlsxwriter
pdf2image>=1.16
numpy>=1.25
opencv-python-headless>=4.8
qrcode>=7.4
Flask-Login==0.6.3
Flask-SQLAlchemy>=3.0
firebase-admin>=6.0
google-cloud-firestore>=2.14.0
cryptography>=41.0
pycryptodome>=3.18
pyzipper>=0.3.6
resend>=0.7.0
zeep>=4.2.1

selenium==4.39.0
webdriver-manager==4.0.2
# Headless browser fallback for dynamic invoice providers (SimpleInvoicing, Megasoft, e-invoicing.gr fallback)
playwright==1.58.0

discord.py>=2.5.0
psutil>=5.9.0

# Reflex frontend
reflex>=0.6.0
httpx>=0.25.0

# NOTE: requirements.txt installs only the Python package.
# Playwright browser binaries are installed by Docker build (see Dockerfile).
# For non-docker installs, run:
#   python -m playwright install chromium
#   sudo apt-get install -y libatk1.0-0t64 libatk-bridge2.0-0t64 libcups2t64 libdrm2 libxss1 libgbm1 libnss3 libx11-xcb1 libxcomposite1 libxdamage1 libxrandr2 libgtk-3-0t64 libasound2t64
